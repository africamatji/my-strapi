'use strict';

/**
 * qualifying-question service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::qualifying-question.qualifying-question');
